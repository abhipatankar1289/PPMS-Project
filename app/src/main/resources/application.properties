spring.config.import=optional:file:.env[.properties]
server.port=${SERVER_PORT:8080}

spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://localhost:3306/ppms?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATASOURCE_USER:root}
spring.datasource.password=${DATASOURCE_PASSWORD:root}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
security.jwt.secret=${JWT_SECRET:0pZhbFOuzzt812sgw5J8JE}