package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.StandardRack_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StandardRack_Repository extends JpaRepository<StandardRack_Entity, Long> {
}