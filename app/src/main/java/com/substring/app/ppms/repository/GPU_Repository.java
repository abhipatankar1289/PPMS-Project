package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.GPU_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GPU_Repository extends JpaRepository<GPU_Entity, Long> {
}