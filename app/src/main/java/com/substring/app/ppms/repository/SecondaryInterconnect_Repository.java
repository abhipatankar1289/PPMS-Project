package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.SecondaryInterconnect_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SecondaryInterconnect_Repository extends JpaRepository<SecondaryInterconnect_Entity, Long> {
}