package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.Interconnect_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Interconnect_Repository extends JpaRepository<Interconnect_Entity, Long> {
}