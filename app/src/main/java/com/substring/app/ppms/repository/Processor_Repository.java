package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.Processor_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Processor_Repository extends JpaRepository<Processor_Entity, Long> {
}