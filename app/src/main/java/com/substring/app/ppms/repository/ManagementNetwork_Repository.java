package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.ManagementNetwork_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ManagementNetwork_Repository extends JpaRepository<ManagementNetwork_Entity, Long> {
}