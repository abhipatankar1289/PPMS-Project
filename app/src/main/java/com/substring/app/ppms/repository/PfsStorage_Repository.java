package com.substring.app.ppms.repository;

import com.substring.app.ppms.entity.PfsStorage_Entity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PfsStorage_Repository extends JpaRepository<PfsStorage_Entity, Long> {
}